import { Router } from "express";

const todoRouter = Router();

// Todoルート一覧（一覧、詳細、作成、更新、削除の順）
// todoRouter.get('/', getAllTodoListHandler);
// todoRouter.get('/:id', getTodoByIdHandler);
// todoRouter.post('/', createTodoHandler);
// todoRouter.put('/:id', updateTodoHandler);
// todoRouter.delete('/:id', deleteTodoHandler);


export default todoRouter;